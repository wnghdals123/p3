# p3

## Agent skills

### Issue tracker

이슈와 스펙은 `.scratch/<feature-slug>/` 아래 로컬 마크다운 파일로 관리합니다. `docs/agents/issue-tracker.md` 참고.

### Triage labels

기본 canonical 어휘를 사용합니다: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. `docs/agents/triage-labels.md` 참고.

### Domain docs

single-context: 리포 루트에 `CONTEXT.md`와 `docs/adr/`. `docs/agents/domain.md` 참고.
